#include "monty.h"
/**
 * f_stack - function name rints the top
 * @head: function parameter stack head
 * @counter: function parameter line_number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
